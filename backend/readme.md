# SETUP
1- Install `composer` in your machine if it's not installed  
2- Run this command in this directory
```bash 
composer install
```
3- Make a copy of `.env.example` and rename it to `.env` and put your connection details